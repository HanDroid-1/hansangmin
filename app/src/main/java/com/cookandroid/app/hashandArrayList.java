package com.cookandroid.app;

import android.widget.TextView;

import java.util.ArrayList;
import java.util.HashMap;

public class hashandArrayList {
    /*TextView txt = (TextView)findViewById(R.id.txt1);

    ArrayList<HashMap<String,String>> mArrayList = new ArrayList<HashMap<String, String>>();

    HashMap<String,String> hashMap = new HashMap<String, String>();

    String TAG_ID = "id";
    String TAG_NAME = "name";
    String TAG_ADDRESS ="address";

        for(int i = 0; i <20; i++ ){

        hashMap.put(TAG_ID, "id" + i );
        hashMap.put(TAG_NAME, "name" + i );
        hashMap.put(TAG_ADDRESS, "address" + i );

        mArrayList.add(hashMap);

    }

        for(int i = 0; i<mArrayList.size(); i++){
        hashMap = mArrayList.get(i);

        txt.setText(hashMap.get(TAG_ID)+" "+hashMap.get(TAG_NAME)+" "+hashMap.get(TAG_ADDRESS));
        }
        */


}

